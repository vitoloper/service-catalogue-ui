export class User {
    jwt?: string;
    user: {
        id: number;
        username: string;
        email: string;
        // password: string;
        // firstName: string;
        // lastName: string;
    }
    // token?: string;
}